import "../styles/Loader.css"; // Ensure to import CSS for styling

const Loader = () => {
  return (
    <div className="loader-overlay">
      <img src="/Evara Logo.png" alt="Loading..." className="loader-img" />
    </div>
  );
};

export default Loader;
